#!/bin/bash
list=$(ls | sed -e 's/^init$//')
dir="$(pwd)"
cd
for i in $list; do
    if [[ -f ".$i" ]]; then
        mv ".$i" ".$i.OLD"
    fi
    if [[ ! -L ".$i" ]]; then
        ln -s "$dir/$i" ".$i"
    fi
done
